# ===================================
# Filter Scope Definitions
# ===================================

scopes:
    name:
        label: redhouse.shelter::lang.view.contact.filter.name
        type: text
        condition: name = :value

    #number:
    #    label: redhouse.shelter::lang.view.contact.filter.number
    #    type: text
    #    scope: FilterContactNumber

    published:
        label: redhouse.shelter::lang.view.contact.filter.published
        type: switch
        conditions:
            - published <> '1'
            - published = '1'
